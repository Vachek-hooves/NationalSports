import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const GuessGameScreen = () => {
  return (
    <View>
      <Text>GuessGameScreen</Text>
    </View>
  );
};

export default GuessGameScreen;

const styles = StyleSheet.create({});
